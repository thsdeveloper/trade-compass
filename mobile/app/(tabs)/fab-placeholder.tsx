import { View } from 'react-native';

// This is a placeholder screen for the FAB button in the tab bar.
// It never renders because the tab button is replaced with an empty view.
export default function FABPlaceholder() {
  return <View />;
}
